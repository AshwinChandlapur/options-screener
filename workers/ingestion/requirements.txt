# Pinned per ADR-0014 / copilot-instructions hard rule:
# new top-level deps require justification in PR body.
#
# httpx                 — Arctic Shift API polling (JSON over HTTPS)
# azure-identity        — DefaultAzureCredential for managed identity
# azure-keyvault-secrets— pull author salt at startup
# azure-eventhub        — publish raw events to reddit-raw-events
# azure-storage-blob    — Blob-first durability before EH publish
# tenacity              — exponential backoff for Arctic Shift 429s / transport errors

httpx==0.27.2
azure-identity==1.17.1
azure-keyvault-secrets==4.8.0
azure-eventhub==5.12.1
azure-storage-blob==12.22.0
tenacity==9.0.0
